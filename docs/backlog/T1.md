# T1 — 저장소 실습 — 점검 기록/Hook 미구현 항목

_source: backlog.json sha256=d127ba6998c2107c468aa977184b2066d43ba8261112ed47659c2441aa37406c, schema=1.1 (as of this doc's generation)_

## 목적

이 프로젝트의 README.md가 소개하는 세 가지 기능(`/inspection-check`, `problem-reader` 서브에이전트, `/hook-check`)이 실제로 구현되고 검증될 때까지의 인프라 작업을 완료하기 위함입니다. 각 하위 태스크는 **개발 환경의 Hook 시스템이 실제로 작동하는지**를 확인하고, **백로그 기반 작업 관리와 문서화 정책**을 정립하는 실습입니다.

## 쉬운 설명

`.claude/` 폴더 안에는 Hook(프로젝트 규칙 자동 검사), 커맨드(명령어 정의), 에이전트(보조 AI) 설정이 들어갑니다. README.md에서 "이런 것들이 있습니다"라고 언급한 것들을 실제로 동작하도록 만들고, 그 과정에서 Windows와 macOS에서 제대로 작동하는지 검증한 후, 각 단계를 문서로 남기는 작업입니다.

## 필요한 입력

- `problem.md` — 프로젝트의 원래 문제 정의서
- `README.md` — 사용자용 가이드(`/inspection-check`, `problem-reader`, `/hook-check` 언급)
- `.claude/settings.json` — Hook 규칙과 권한 설정
- `CLAUDE.md` — 작업 절차와 규칙
- `.claude/agents/` 및 `.claude/commands/` 디렉터리

## 결과물

- `.claude/commands/hook-check.md` — Hook 보호 파일 검증 커맨드 정의
- `.claude/agents/critical-reviewer.md` — 코드 검토 보조 에이전트
- `.claude/agents/backlog-explainer.md` — 백로그 문서화 보조 에이전트
- `tools/backlog-cli.mjs` — 백로그 조회/변경 CLI 도구
- `docs/backlog/T1-1.md` 부터 `T1-4.md` — 각 하위 태스크 설명 문서

## 선행 작업

없음 — 이 태스크는 프로젝트의 기초 인프라 작업입니다.

## 수행 순서

1. **T1-1 (Windows Hook 검증)**: `settings.json` 안의 Hook 규칙이 실제 Windows 환경(PowerShell)에서 작동하는지 확인
2. **T1-2 (/inspection-check 구현)**: `data/inspection.csv`의 메모 누락을 자동 점검하는 커맨드 구현
3. **T1-3 (problem-reader 에이전트 등록)**: 백로그 제약으로 문제 정의서를 읽을 수 있는 보조 에이전트 구현
4. **T1-4 (/hook-check 커맨드 검증)**: Hook 보호 장치가 실제로 파일을 지키는지 실습·시연

## done_when 확인 방법

**done_when**: T1-1~T1-4 전부 `done`

하위 태스크 상태 확인:
```bash
node tools/backlog-cli.mjs show T1-1
node tools/backlog-cli.mjs show T1-2
node tools/backlog-cli.mjs show T1-3
node tools/backlog-cli.mjs show T1-4
```

모든 태스크의 `status` 필드가 `"done"`이면 이 에픽 태스크의 done_when이 충족됩니다.

## 확인 질문

없음. 이 태스크는 상위 에픽이며, 각 하위 태스크(`T1-1`~`T1-4`)의 상세 설명은 `docs/backlog/` 폴더의 개별 문서에서 확인할 수 있습니다.
