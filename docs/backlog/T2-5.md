# T2-5 — Codex CLI 판단 기준 가이드라인 작성

_source: backlog.json sha256=9915647…, schema=1.1 (as of this doc's generation)_

## 상태

**done** (2026-09-15 06:12:07 UTC)  
critical-reviewer 2라운드 검토 완료. 모든 Critical/High 지적이 해소되었으며, npm run check 통과.

## 목적

Codex CLI가 gradient vanishing, entropy collapse, loss-reward 불일치 세 가지 이상 유형을 판단할 때 따를 구체적인 기준을 problem.md §9에 문서화한다.

문제 정의서에서는 이 3가지를 "AI(Codex CLI)가 판단"하는 항목으로 지정했지만, 판단 기준은 아직 구체적 수치/조건이 없었다. 이 작업은 그 기준을 명확히 하고, 판정 절차와 Codex CLI의 입출력 형식을 표준화하는 것이다.

## 쉬운 설명

학습 중 감지해야 할 이상 5가지:
1. **NaN/Inf loss** — 규칙 기반 (즉시, 명확)
2. **Gradient explosion** — 규칙 기반 (임계치 비교)
3-5. **Gradient vanishing, entropy collapse, loss-reward 불일치** — AI 판단 필요 (추이 분석, 상관도 등)

앞의 2가지는 `if` 문으로 명확히 판정 가능하지만, 뒤의 3가지는 여러 epoch 데이터의 추세와 통계를 봐야 하므로 Codex CLI(대언어모델)에게 맡기기로 했다. 다만 Codex가 무작정 판단하는 것이 아니라, **명확한 기준과 수치를 제시해 일관성 있게 동작**하도록 하는 것이 이 작업이다.

## 필요한 입력

- **problem.md §3-5**: 범위 정의, 과거 사례, 확인된 사실
- **problem.md §6**: T2-1/T2-2 결정 사항 (gradient explosion 임계치, 정상 run 기준)
- **problem.md §7**: entropy collapse 예시 사례
- **실제 학습 로그 샘플**: T2-7에서 실행 시 확인 (형식 검증)

## 결과물

**problem.md §9 — Codex 판단 기준 가이드라인** (완성)

```
### 9.1 Gradient vanishing (gradient 소실)

정의: 학습 신호(policy gradient)가 사실상 사라져 파라미터가 갱신되지 않는 상태.

판단 신호 (아래 두 조건을 모두 만족할 때 이상으로 본다):
  - 최근 N epoch(초기값 10, `codex.vanishing.window_epochs`) 동안 
    gradient_norm이 baseline 평균의 일정 비율(초기값 1%, `codex.vanishing.ratio`) 
    미만으로 지속됨.
  
  - 같은 구간 동안 policy_loss의 변동폭(구간 내 최댓값-최솟값)이 
    baseline policy_loss 평균의 일정 비율(초기값 2%, 
    `codex.vanishing.loss_plateau_ratio`) 미만 — 사실상 정체.

오탐 방지: 학습 초반(초기값: 처음 5epoch, `codex.vanishing.warmup_epochs`)은 제외.

### 9.2 Entropy collapse (탐색 붕괴)

정의: 정책의 확률 분포가 지나치게 빨리 결정론적으로 수렴해 탐색이 멈춘 상태.

판단 신호 — 아래 (A AND B) 또는 C 중 하나라도 성립하면 이상으로 본다:

  A: entropy가 최근 N epoch(초기값 10, `codex.entropy_collapse.window_epochs`) 
     동안 지속적으로 감소하는 추세(단조 감소에 가까움).
  
  B: 절대값이 `min(baseline 평균 × 0.1, 0.05)` 
     (`codex.entropy_collapse.ratio`=0.1, `codex.entropy_collapse.abs_cap`=0.05 
     — 이 수식은 baseline 평균과 무관하게 임계치가 항상 0.05를 넘지 않는다) 
     미만으로 내려감.
  
  C(급락 — 단독으로도 이상 신호): 최근 `drop_window_epochs`(초기값 12, 
    `codex.entropy_collapse.drop_window_epochs`) 동안 구간 시작값 대비 
    `drop_ratio`(초기값 80%, `codex.entropy_collapse.drop_ratio`) 이상 감소.

정상 판단 기준: (A AND B)도 C도 성립하지 않으면 정상.

### 9.3 Loss-reward 불일치

정의: reward와 policy_loss가 기대되는 역상관 관계(reward 상승 ↔ loss 하락, 
reward 하락 ↔ loss 상승)에서 벗어나, 두 지표가 같은 방향(부호)으로 함께 
움직이는 상태.

계산 절차: 최근 N epoch(초기값 10, `codex.loss_reward_mismatch.window_epochs`) 
동안 reward와 policy_loss 각각에 대해 최소자승 선형회귀 기울기를 구하고, 
기울기×window_epochs로 "구간 전체 변화량"을 계산한다.

판단 신호 (아래 두 조건을 모두 만족할 때 이상으로 본다):
  - reward의 구간 전체 변화량 절댓값이 baseline reward 표준편차의 0.5배
    (초기값, `codex.loss_reward_mismatch.reward_change_min`) 이상 
    — reward가 "명확한 추세"를 보여야 판단 대상이 된다.
  
  - reward 기울기와 policy_loss 기울기의 부호가 같음
    (둘 다 양수 = 함께 상승, 둘 다 음수 = 함께 하락).

추가 설명: gradient_norm이 정상 범위(explosion도 vanishing도 아님)인데 
이런 불일치가 나타나면, "망 설계/파라미터 문제"보다 "reward 신호 자체의 문제"
일 가능성이 높다는 설명을 판단 근거 문장에 포함한다.

### 공통 지침

- Codex는 판단 결과의 `reason` 필드에 반드시 어떤 수치를 근거로 이상/정상이라고 
  판단했는지를 포함해야 한다.

- 다중 이상 동시 발생 처리: 규칙 기반(NaN/Inf, gradient explosion)과 
  AI 판단(vanishing/entropy collapse/loss-reward 불일치)은 서로 다른 유형이라 
  자동으로 겹치지 않지만, 같은 epoch에 여러 유형이 동시에 이상으로 판정될 수 있다 
  — 이 경우 알림을 유형별로 각각 보내지 않고 한 텍스트 파일에 모든 유형을 함께 
  기록한다.
```

**추가 산출물**: Codex CLI 호출 형식 표준화
- 입력: 로그 파일 경로 + 현재 epoch 번호 + 위 기준 요약
- 출력: JSON 한 줄 (파싱 용이)
  ```json
  {"anomaly": true|false, "type": "vanishing|entropy_collapse|loss_reward_mismatch|null", "reason": "판단 근거 수치를 포함한 설명 문장"}
  ```

## 선행 작업

**T2-1, T2-2** (이미 완료된 decision 태스크)
- T2-1: gradient explosion 임계치 산출 방식 결정
- T2-2: "정상 run" 판별 기준 결정
- 이 두 작업의 결정이 없으면 baseline 수치를 비교할 기준이 없음

## 수행 순서

이미 완료된 단계:

1. ✓ T2-1, T2-2 결과 검토 (baseline 설정)
2. ✓ 각 판단 유형별 정량적 기준 수립 (수치 임계값, 윈도우 크기 등)
3. ✓ Codex CLI 입출력 형식 표준화 (JSON 파싱 용이하도록)
4. ✓ problem.md §9 전체 작성 및 critical-reviewer 2라운드 검토 완료
5. ✓ npm run check 통과 (문법, lint, 길이 검사)

## done_when 확인 방법

**원문**: "3개 판단 유형별 구체적 기준 문서 작성 완료"

**완료 확인**:
1. ✓ problem.md §9가 완성되고, 다음을 포함:
   - 9.1 Gradient vanishing: 정의, 판단 신호 (2가지 조건), 오탐 방지, 설정 키 명시
   - 9.2 Entropy collapse: 정의, 판단 신호 ((A AND B) 또는 C), 정상 판단 기준
   - 9.3 Loss-reward 불일치: 정의, 계산 절차, 판단 신호 (2가지 조건), 설명 추가
   - 공통 지침: Codex 출력 형식, 다중 이상 처리

2. ✓ 각 판단 유형별 설정 키 (`config.yaml`에 저장될) 명시:
   - `codex.vanishing.window_epochs`, `codex.vanishing.ratio`, `codex.vanishing.loss_plateau_ratio`, `codex.vanishing.warmup_epochs`
   - `codex.entropy_collapse.window_epochs`, `.ratio`, `.abs_cap`, `.drop_window_epochs`, `.drop_ratio`
   - `codex.loss_reward_mismatch.window_epochs`, `.reward_change_min`

3. ✓ Codex CLI 입출력 형식 표준화:
   - 입력: 로그 파일 경로, 현재 epoch, 기준 요약
   - 출력: `{"anomaly": bool, "type": str, "reason": str}` JSON 형식

4. ✓ Critical-reviewer 검토 완료: Critical 1개, High 6개 → 모두 수정
5. ✓ 2라운드 재검증: High 4개 추가 수정, npm run check 통과

**확인 방법**:
```bash
# 문서화 확인
grep -A 50 "^### 9.1" problem.md    # Gradient vanishing 섹션
grep -A 50 "^### 9.2" problem.md    # Entropy collapse 섹션
grep -A 30 "^### 9.3" problem.md    # Loss-reward 불일치 섹션

# 설정 키 확인
grep "codex\." problem.md | head -20

# 코드 검사 (내용 작성 후)
npm run check
```

## 알려진 한계

이 작업을 완료한 후에도 **아직 구현되지 않은 항목**이 있습니다:

### 1. Codex 프롬프트 전문 미등록
- **문제**: §9에서 정의한 판단 기준은 자연어로 기술되어 있습니다.
- **필요한 작업**: 이 기준을 실제 Codex CLI가 사용할 구체적인 프롬프트로 변환하는 별도 태스크가 필요합니다.
  - 각 판단 유형별 상세 프롬프트 작성
  - Codex의 context window 내에 들어갈 수 있도록 최적화
  - 출력 형식 검증 로직
- **상태**: 이 후속 태스크는 현재 backlog에 등록되지 않았습니다(이 문서의 결과물 범위 밖).

### 2. 의존성(deps) 상태의 해석
- **표시**: T2-5는 `deps: ["T2-1", "T2-2"]`를 가집니다.
- **문제**: T2-1과 T2-2는 `category=decision`이므로 CLI 상에서 영구히 `status=todo`로 남아있습니다(자동 `done` 불가).
- **실제 상황**: 하지만 **실질적으로는 두 태스크의 결정이 모두 완료**되어 있습니다:
  - T2-1: 평균 + 5×표준편차 기준 확정
  - T2-2: 자동 필터 3가지 + 사람 최종 승인 기준 확정
  - 이 결정들은 `evidence` 필드와 `problem.md`에 이미 기록됨
- **따라서**: T2-5는 실제로 진행 가능했고, 진행되었으며, 완료되었습니다(CLI의 "blocked" 표시와 무관하게).

## 필요한 입력 (구현 단계)

T2-7에서 코드 구현 시 다음을 활용:
- **문제 정의서 §9.1~9.3**: 각 판단 유형의 정량적 조건
- **설정 파일 (`config.yaml`)**: 초기값(window_epochs, 임계치 등)
- **로그 포맷**: 실제 log 파일의 epoch 단위 구조 (T2-7 구현 시 확인)

## 확인 질문

**주요 결정이 모두 완료되었으므로 추가 확인 질문은 없습니다.**

다만, **구현 단계(T2-7, Codex CLI 통합 태스크)에서 필요할 사항**:

1. **Codex 접근 방식**: 각 판단 유형을 Codex CLI에 **별도 호출**로 수행할 것인가, 아니면 한 번에 3가지를 물어볼 것인가? (epoch당 Codex 호출 횟수/지연 영향)

2. **초기값 조정 절차**: config.yaml의 임계값들(window_epochs=10, ratio=1% 등)을 baseline 5회 관찰 후 어떻게 조정할 것인가? (누가 판단, 어떤 메트릭 기준)

3. **출력 검증**: Codex가 반환한 JSON이 예상 형식인지 검증하고, 형식 오류 시 폴백(fallback) 처리는? (예: 파싱 실패 시 판단 스킵 vs 오류 기록)

4. **로그 포맷 호환성**: 현재 entropy/reward 텍스트 로그의 정확한 포맷이 파싱 가능한가? (problem.md §8의 미확인 사항)
